import java.util.*;
public class ex81 {
	public static void main (String args[]) {
	Scanner sc = new Scanner(System.in); 
	
		int y,uC=0,lC=0;
		int z = s.length();
		String s = sc.nextLine();
		
		
		for(int y = 0; y <= z; y++){
         if(Character.isUpperCase(s.charAt(y))){
            char w = s.charAt(y);
            s = sq + w + " ";
         }
		}
		
		

	
    if (Character.isUpperCase(input.charAt(k))) uC++;

    
    if (Character.isLowerCase(input.charAt(k))) lC++;

	System.out.println("Numero de caracteres maiusculos " + lmai );
	System.out.println("Numero de caracteres minusculos " + lmin );
	System.out.println("Numero de caracteres numericos "  );
	}
}
